using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class sl_SetParentDish : MonoBehaviour
{
    public string parentName;

    void Start()
    {
        //this.gameObject.transform.SetParent(GameObject.Find(parentName).transform, false);
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
